# Rulings of 2026-09-22

Made by Hamm in one sitting, on the evidence each item carried. Each ruling names the work it
creates; that work is executed through `artifacts/todo_stack.md` and closes the problem rows
named. Durable rules among them are in `artifacts/fact_stack.md`.

| Item | Ruling | Work it creates |
|---|---|---|
| 06-67, P-157, P-158 | **(d) event flag.** `jnwb_waived_requirements` records what was actually waived on this read, not what was requested. No value changes; no read starts or stops failing | 06-82 implements: the flag, the six-state table in `docs/errors.md`, one test per row |
| 06-13, P-104, P-105, P-107 | **(b), staged.** 0.2.6: a `compress_fp32` call that names no selection emits `FutureWarning` and keeps the current preset. 0.2.7: `select=` is required | An implementation item for `select=` and the warning; `CHANGELOG.md` deprecation entry |
| Corpus access (P-54; 06-31, 06-32, 06-99, 06-13 real-corpus cells) | **Granted, read-only.** Agents may read the raw NWB on `D:`, write outputs only to `E:` or the session scratchpad, and never copy a corpus identifier into `jnwb/`, `docs/`, `skills/` or `tests/` | The four blocked items become executable |
| 06-92, P-41 | **Every declared version.** The fact-stack sentence is replaced (applied the same day) | none further |
| P-114 | **Refuse now, deliver in 0.2.7.** `aggregate_to_db(how="mean_of_ratios")` on trial-averaged accumulator input raises `ValueError` naming the estimand it cannot deliver | 0.2.6 refusal and test; 0.2.7 per-trial ratio path in `artifacts/planned_post_0.2.6.md` |
| P-91 | **Add `correction: "none"`** to `exploratory_compare` and `exploratory_multi` results; the `multiple_comparison` block stays off the exploratory surface | Additive API change, tests updated, `CHANGELOG.md` Added entry |
| P-92, P-93 | **Add `method=`** (keyword-only, `"both"`, `"pearson"`, `"spearman"`; default preserves behaviour) to `correlate` and `exploratory_correlate`. **Every additive public API change gets a `CHANGELOG.md` Added entry**; no deprecation path is needed for one | The parameter, skill rows, docs, tests; Added entries for 06-45 and this |
| P-68 | **Widen gate 8** to hold `README.md` and `docs/install.md` to the interpreter set. `goal.md` stands | Gate 8 extension with a discriminator |
| P-96 | **Define both terms of each pair, once, in one glossary**: operation; workflow (jnwb ships no pipeline); session = one NWB file, recording = a continuous series within it; contact = a physical site, channel = a data row; electrode = one row, electrodes table = the NWB table | Glossary page; each page uses the defined term |
| P-34, P-127 | **Big-O from the reference.** The order document records upper bounds justified by the algorithm and its published reference, and drops "verified". Timed exponents stay a separate, labelled benchmark with at least three input scales per row | Rewrite of `artifacts/benchmarks/complexity_inventory.md` and the order docs (06-86, 06-58) |
| P-122 | **`groups=` in 0.2.7**, through the capability-gated route. 0.2.6 keeps the documentation and `assign_outer_folds` | Entry in `artifacts/planned_post_0.2.6.md` |
| 06-101, P-83 | **Fix the field, staged.** `JRSAResult.p` and `.q` become 0-d arrays; indexing `p[0]` keeps working in 0.2.6 through a shim that emits `FutureWarning`, removed in 0.2.7 | The field change, the shim, a test pinning both shapes, `CHANGELOG.md` Changed and Deprecated entries |
| 06-05 | **Freeze the Acceptance section as written**, once 06-13's implementation item exists. Each line is re-established against the live tree at freezing; a line that cannot be reproduced does not enter the set | The freeze |
| P-03 (1) | The role definitions in `artifacts/agents/` are **public**, linked from `artifacts/agents.md`; they stay out of the wheel | Link added the same day |
| P-03 (2), (3) | A table titled "Representative API" **may omit** modules while `docs/api.md` is complete. **One test taxonomy**: the probe classes of `CONTRIBUTING.md` "Testing rule". The five-tier structure the finding cites lives only in a presentation slide outside the repository, so nothing in the repository is deleted; the question as asked named `CONTRIBUTING.md` in error | none in the repository |
| Unintegrated worktree changes | **Triage, then clean.** Each tree is diffed against `dev`; changes already on `dev` are marked, the rest listed with what they fix. Hamm approves which to integrate as fresh, re-verified commits; the trees are then removed | Triage report |
| R-2, 06-25 | **Decline stays in 0.2.6.** Skill routing tests for all four outcomes, declining included, are 0.2.6 work | 06-25; `artifacts/goal.md` §3 updated |
| R-3, 06-38 | **Verify from a TestPyPI candidate.** The candidate is published to TestPyPI and verified from there in a clean environment before production publication, and from PyPI after | 06-38; fact "Release publication ordering" updated |
| 06-127, `jnwb.vis` | **Optional extra in 0.2.6.** `jnwb.vis` ships behind `pip install jnwb[vis]`; `import jnwb` works without `plotly` | 06-127 |
| P-180, skill count | **Twelve planned skills.** `jnwb-landmark-viz` is a 0.2.6 skill: ten in 0.2.6, plus `jnwb-paradigm` and `jnwb-qc` planned | Fact "Skill creation is capability-gated" updated |
| P-180, 06-130 | **Revert on `main` first.** `9d738211` is reverted on `main` in its own pull request, so the release pull request is a clean merge of `dev` | 06-130 |
