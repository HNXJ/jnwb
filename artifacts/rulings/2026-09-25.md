# Rulings of 2026-09-25

Made by Hamm on the evidence of the two post-release inspections at `e66e70a9` (07-03). Each
ruling names the work it creates; that work is executed through `artifacts/todo_stack.md`.

| Item | Ruling | Work it creates |
|---|---|---|
| Blocker candidates found in the shipped 0.2.6 | **A 0.2.6.1 patch**, cut from `dev` and released through one pull request into `main` as 0.2.6 was. It carries the silently wrong results and the misleading documentation; the rest of 07-03 stays 0.2.7 work | 06-201 to 06-205 |
| `jrsa` permutation null for paired metrics on a time axis | **Circular shift by default.** A `null=` parameter takes `'circular_shift'` (the default, which keeps each series' autocorrelation), `'block'`, or `'iid'`, which must be named to be used. The result records the scheme. p-values change and `CHANGELOG.md` says so | 06-201 |
| `transfer_entropy(estimator='symbolic')` | **Refused.** It raises with a message pointing to `'quantile'` until a calibrated null exists; the calibrated null is 0.2.7 work. A break for its callers, announced in `CHANGELOG.md` | 06-202 |
| `jrsa` row metrics (`rsa`, `cka`, `rv`, `hsic`, `distance_correlation`, `procrustes`) whose axis 0 is time | **Warn now, require in 0.2.7.** The default stays the row permutation, so 0.2.6 numbers hold; forming a null without naming `null=` warns that axis-0 time needs `'circular_shift'` or `'block'`. From 0.2.7 these metrics must name `null=`. The critic measured a false-positive rate of 1.000 for `cka` and `rv` on independent AR(1) data laid out time by channel | 06-206 |
| `jrsa` `bootstrap=` for paired metrics | **Refused unless `null='iid'` is named**, which declares the samples exchangeable. A 95% interval covered the true zero in 0.47 of independent AR(1) pairs at phi 0.9. A calibrated block bootstrap is 0.2.7 work | 06-207 |
| Directed-estimator surrogates with few trials | **Circular shift below 7 trials** (it was below 3), recorded as `params['surrogate_scheme']`. The trial permutation has 2 distinct pairings at 3 trials and rejected at 0.05 in 0.30 to 0.43 of independent-noise tests; 7 trials and more are unchanged | 06-208 |
| Concurrent agents | **One integrator, any number of workers.** The local session is the only writer on `dev`; a worker reviews, executes claimed packets in its own clone and hands back bundles, and the shared project-state files are the only shared truth. The protocol lives in `artifacts/cooperation.md`; stack hygiene runs daily and memory curation weekly, on a schedule | `artifacts/cooperation.md` |
| Amendments to the cooperation protocol | **Accepted as proposed by the worker.** Ids are qualified by sender; a worker's clone may fetch from the integrator's tree; T1 runs T2 over the commits since the last T1 and posts a one-line "no change" when `dev` has not moved; the worker scans bus and chat friction for rule proposals instead of curating memory it does not own; its scheduled runs start fresh. T1 daily at 07:47, the friction scan Mondays at 08:47. Chat runs in jchat file mode only, with no hub, port or MCP registration, and carries conversation, never claims | `artifacts/cooperation.md` |
| Automation of the standard tasks | **None.** No agent runs a task on a schedule; every standard task in `artifacts/cooperation.md` runs on request, from Hamm or from another agent through the bus or chat. The work stays integrated on this PC across all agents. This withdraws the schedule in the two rows above | `artifacts/cooperation.md` |
| `jrsa` `lag` for the six metrics with observations on axis 0 (`cka`, `rv`, `rsa`, `procrustes`, `distance_correlation`, `hsic`) | **Lag along axis 0**, circular like the paired metrics. It rolled the feature axis, which leaves all six invariant, so every lag returned one value. Values at a nonzero lag change, and `CHANGELOG.md` says so | IA-04 |
| A downstream paper-reproduction agent | **Lives downstream**, pinned to a jnwb release, with its claim registry, decline tree, reference outputs and scorer. jnwb gains only what such an agent cannot do without and every NWB consumer can use, as 07-05 | 07-05 |
| The analysis preflight | **A public API** that returns one of the four outcomes of `artifacts/direction.md` with the reason and the missing inputs as data, so decline accuracy can be scored by a script | 07-04, 07-05 |
| The declared benchmark and a paper-reproduction run | **Kept separate.** The benchmark stays as declared; a paper agent's with-and-without-jnwb comparison is reported downstream | `artifacts/planned_post_0.2.6.md` |
| `skills/jnwb-fact-action` | **Stops shipping.** It is a contributor-process skill; it moves to where this repository's own agents read it | IB-17 |
| `jrsa` `lag` wrap-around | **Truncate to the overlap.** A lag compares the overlapping samples only; the sample count shrinks by the lag and is recorded. Values at a nonzero lag change for every metric, and `CHANGELOG.md` says so. This supersedes "circularly" in the axis-0 row above | IA-06 |
| Depth unit in the laminar plots of `jnwb.vis` | **A required `depth_unit=`** (`'mm'`, `'um'` or `'relative'`) with no default; the unit is never inferred from the data range | IB-14 |
| Millimetre depth-axis label | **"Cortical Depth (mm)"**, the counterpart of the existing "Cortical Depth (μm)"; graded highly recommended and taken under the decision grades | IB-14 |
| Where the process skill lives, and gate 2 | **`artifacts/skills/jnwb-fact-action/SKILL.md`**; gate 2 accepts a `SKILL.md` directly under `artifacts/skills/<name>/` only when no shipped skill has that name. Graded highly recommended and taken under the decision grades | IB-17 |
| The release gate and the sdist's working rules | **`AGENTS.md` and `jnwb-fact-action` are forbidden archive components**, so a manifest change that ships either fails on the built archive. Graded highly recommended and taken under the decision grades | P-310 |
| Gate 14 and the agent entry page | **`artifacts/agents.md` is scanned like `README.md`**, since `README.md` links it. Graded highly recommended and taken under the decision grades | IB-27 |
| Preflight signature | **`jnwb.preflight(question)`**, a function taking a `Question` and returning a `Preflight`; `Question` stays pure data and gains optional fields | 07-07 |
| Preflight decline | **Caller-declared.** The caller states the unsupported inference and the preflight reports it; jnwb encodes no claim vocabulary | 07-07 |
| Preflight failure | **Allowed before execution when the caller declares the result non-identifiable**, so all four outcomes are reachable | 07-07 |
| Preflight required fields | **A minimal core** (`signals`, `signal_units`, `contrast`, `inference_unit`) is required, else `request`; the other fields are optional and reported when absent. `signal_units` avoids the clash with the neural unit of `inference_unit` | 07-07 |
| Preflight field names | **`unsupported_inference` and `non_identifiable`** (strings, the wording of `artifacts/direction.md`), **`signal_units`** as a mapping from signal to unit, and `data`, `paradigm`, `verification_plan`, `axes`, `conditions`, `required_skills`; the goal reuses `hypothesis`. Graded highly recommended | 07-07 |
| Streaming mean of ratios | **Each trial's baseline arrives with the trial** (`TFRAccumulator.add_trial(..., baseline=)`) and `mean_of_ratios()` returns the linear mean, decibels taken once by `to_db`. A common baseline makes the estimand the ratio of means, and a baseline arriving after the trials needs them held, so this design dominates. Graded highly recommended | 07-15 |
| Skill template names | **The template row reads "Invariants & Safeguards"**, matching the eight domain skills, and the router's form is the template plus Execution (graded highly recommended) | 07-06 |
| Grouped outer folds in `nested_cv_linear_svm` | **`StratifiedGroupKFold` over group ids relabelled by a seeded permutation, with `shuffle=False`**, because its own shuffle does not stratify before scikit-learn 1.8; the folds are identical on 1.3.1 and 1.8.0. Graded highly recommended (85) and taken under the decision grades | 07-16 |
| The inner search under `groups=` | **Grouped as well**, over the outer training set's groups, so no group informs both the choice of `C` and its validation. Graded highly recommended (80) | 07-16 |
| Too few groups, or one class in an outer training fold | **Fewer than two groups returns status `"insufficient_groups_for_cv"`, as the ungrouped path does for too few trials; an outer training fold holding one class raises**, since no classifier can be fitted and a chance number would be silent. Graded highly recommended (85) | 07-16 |
| Release scope of 0.2.7 | **Everything in the todo stack**, one coherent release; the stack is not split into a 0.2.8 | all items |
| Per-frequency baseline shape | **Refused unless its `ndim` is 0 or the data's**, in `TFRAccumulator.add_trial(baseline=)` and `aggregate_to_db`; the message suggests `baseline[:, None]` | IB-68 |
| Preflight blank names and unit types | **A blank signal name is not stated** and gives `request`; **a non-string unit raises `TypeError`**, as other non-string declarations do | IB-65 |
| `classify_response_significance` | **A real test of response against baseline**, whose p falls with trial count; the effect-size-derived normal p goes | IA-18 |
| Crossover depth frame | **Both, named**: `crossover_depth_um` in the rank-times-pitch frame of `label_layers`, and a separate field for absolute z when the geometry has it | IA-13 |
| Checkout-only files in the shipped router | **Dropped**; contributor checks live in `CONTRIBUTING.md` | IB-62 |
| Size of `AGENTS.md` | **Recipes to a tested docs page, history paragraphs to the rulings**, with pointers left behind | IB-38 |
| `dev` deleted by the release merge | **The admin bypass of the `dev` deletion rule is removed**; automatic deletion of merged heads stays | RP-1 |
| TestPyPI event | **The tag push publishes to TestPyPI**, before the release event publishes to PyPI | RP-5 |
| Decision grades | **Grade each option 0 to 100 and act by grade** (`AGENTS.md` §12): at 80 and above act and record it; 40 to 80 build the minimal base and flag the rest; below 40 a todo. Two defensible options grade no higher than recommended and come to Hamm | `AGENTS.md` §12 |
