# Rulings of 2026-09-24

Made by Hamm on the evidence each item carried. Each ruling names the work it creates; that work
is executed through `artifacts/todo_stack.md`.

| Item | Ruling | Work it creates |
|---|---|---|
| Laminar curation and labelling (asked for by the omission project) | **Omission contributes it, as composable parts.** After 0.2.6 is tagged, omission opens a pull request against `dev`: bad-channel detection with a neighbour window, run-length interpolation, an evoked-CSD sink test, and the fusion of the vFLIP, xFLIP and CSD estimates, each a public function, plus one thin orchestrator that returns a result object. Every threshold is a parameter with a cited default; "WM" and "outside cortex" join the label set; no project vocabulary enters jnwb | A 0.2.7 entry to review the pull request against the library rules |
| Manual layer labels written into NWB files | **jnwb stays read-only.** Labels are written through `pynwb` directly, as an electrodes column or a sidecar file, and jnwb only reads them | None in jnwb |
