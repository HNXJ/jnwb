# Rulings of 2026-09-23

Made by Hamm on the evidence each item carried. Each ruling names the work it creates; that work
is executed through `artifacts/todo_stack.md`.

| Item | Ruling | Work it creates |
|---|---|---|
| P-21, 06-16 | **Rename, deprecate.** The geometric column that `enrich_units_dataframe` and `get_all_units_metadata` emit as `layer` is emitted as `depth_class`. `layer` stays in 0.2.6 as a duplicate of it, and reading it emits `FutureWarning`; it is removed in 0.2.7 | 06-16: the rename, the duplicate column and its warning, tests, docs and skill rows; `CHANGELOG.md` Changed and Deprecated entries; the 0.2.7 removal in `artifacts/planned_post_0.2.6.md` |
| P-178, lane `fig` | **Take the captions, widen 06-52.** The lane's figure captions integrate onto `dev` (with the 06 onset caption saying the gap *includes* the estimator group delay); its known-failure lists and its contract-page rewrite do not; 06-52 gains the write set its repair needs: the figure generator, the rasters, the quickstart script and `docs/_theme_override.css`. The lane is removed | 06-52 widened; the lane removed |
| 06-05 | **Freeze now.** The Acceptance section is re-established against the live tree and frozen | The freeze, applied the same day |
| P-226 | **Flip to match the phase slope index.** `imaginary_coherency` takes the cross-spectrum as `<X Y*>`, so a positive value means x leads for it and for `phase_slope_index` alike; magnitudes are unchanged | The sign change, its discriminator, docstring and skill rows; a `CHANGELOG.md` Changed entry |
| 06-114 follow-up | **Floats only.** `compress_fp32(select=)` refuses integer and boolean datasets as it refuses groups, before anything is written | The refusal, its discriminator, the skill row and the CHANGELOG Added entry re-worded |
| Release | **Released by the agent once done.** When the acceptance set holds, the agent carries 06-130 and 06-40 through, including the revert pull request on `main`, the tag and publication, and verification from the index. Up to three agents run in parallel | 06-130 and 06-40 run at `AUTONOMY: max` inside this authority |
| 06-130 timing | **Revert on `main` now.** `main` still draws a default 0.405 crossover depth from `9d738211`, so the revert pull request opens ahead of the closure order rather than after 06-60 | HNXJ/jnwb#20, merged once CI is green; 06-130 keeps only the check that the release merge tree equals `dev` |
| 06-56 Q1 (P-62) | **Drop the GPU paths.** `band_power` and `relative_power` compute on the CPU; `device='cuda'` or `'metal'` warns through the one resolver. Signatures unchanged | The removal, its discriminators, the skill GPU line and a `CHANGELOG.md` entry, in the 06-56 lane |
| P-252 `df` | **NaN like its siblings.** Where a comparison has no estimate and its statistic, p and effect size are NaN, its `df` is float NaN as well; a normal case keeps its integer `df` | The change in 06-141's lane, its discriminator, docstrings and CHANGELOG |
| P-114 copy limit | **Meets the 2026-09-22 ruling.** A view of a trial-averaged array raises; a real copy (`np.array`, assignment, a `write()` round trip) carries no mark, and the docstring and `CHANGELOG.md` say so | The limit stated in the docstring and CHANGELOG; P-114 closes on independent verification |
| N1 of the fifth verification pass (P-286) | **Read-only.** `nwb_read_io` accepts only `mode='r'` and raises on any other mode before the file is opened; a write goes through `pynwb.NWBHDF5IO` directly. `goal.md` §5 stands unamended | The refusal, its discriminator, the docstring, and the `CHANGELOG.md` entry, in the r6 lane |
