# Problem stack

Opened 2026-09-19. Every problem detected from this date until the release that empties it.
Companion to `artifacts/todo_stack.md`: the todo stack holds work that was planned, this holds
defects, contradictions and unknowns that were found. A problem may become a todo item; it is
not deleted when it does, it is dispositioned to the item that answers it.

## The rule

A release requires this file to hold no `open` problem. **Three** dispositions close one; `open` is listed first because it is the state they close *from*, and calling it a closing disposition was this file contradicting both itself and `AGENTS.md` §2 (recorded as P-69):

| Disposition | Meaning |
|---|---|
| `open` | Not yet answered. **Blocks the release.** |
| `repaired` | Fixed, with the commit and the check that would catch its return. |
| `accepted` | Real, not repairable, and the reason is recorded. A published artifact cannot be un-published. |
| `not-a-defect` | Investigated and the premise is false. The evidence that killed it is recorded. |

`accepted` is the dangerous one. It closes a problem without fixing it, so it carries a reason
that would survive someone hostile reading it, and it is never used for something merely
inconvenient to repair.

Emptiness is a fixpoint, ruled 2026-09-19: the release opens when one full pass over the
documentation, the code and both stacks discovers no new problem. Problems found during that
pass re-open it. A pass that finds nothing is the terminating condition, not a date.

## Open

| ID | Problem | Found by | Answered in |
|---|---|---|---|
| P-01 | `scripts/docs_build.py` writes `site/` inside the repository, so no read-only packet can establish that the strict docs build passes. Build status is unknown, not passing. | 06-04 | 06-36 |
| P-02 | 16 review findings reproduce on the live tree and no stack item claims them. | 06-03 | 06-17 |
| P-03 | Three findings need a human ruling on scope before they can be dispositioned: whether the amended agent-vocabulary rule permits the repair that `ai-plumbing/agent-roles-exist-but-ship-nowhere` implies; whether a table declaring itself "Representative API" may omit a module; whether a superseded five-tier test taxonomy still makes `reliability/contributing-taxonomy-is-a-different-one` a defect. | 06-03 | Human ruling, then 06-17 |
| P-04 | `dist/` holds 0.1.1, 0.1.3 and 0.2.4 and no 0.2.5 build, so the distribution-cleanliness test inspects three superseded releases and never the one that shipped. | 06-04 | 06-37 |
| P-06 | `.venv\Scripts\python.exe` collects 2841 tests with 1 error (statsmodels absent); `C:\Python314` collects 2879 with none. Two interpreters disagree on what the suite is. | 06-03 | 06-35 |
| P-19 | `docs/10_operation_specifications.md:91` describes `label_layers` as returning `Dict[int, str]` "mapping channel index". It is keyed on `probe_geometry.channel_ids`, which are `str` under `synth_laminar_motif`, and they are identifiers rather than indices. Both the type and the word are wrong. | 06-43 | Open — unclaimed; fits Batch 5 **Claimed by 06-77.** |
| P-20 | The unit-to-layer composition works through existing exports and no document or skill shows it. The capability exists and is unreachable by reading. | 06-43 | Open — unclaimed; a documentation item, not an API one **Claimed by 06-89.** |
| P-21 | `metadata.get_all_units_metadata` emits a `layer` column that silently reads `Unknown`, and `addressing.enrich_units_dataframe` writes a column literally named `layer` carrying the geometric taxonomy, not the electrophysiological one. Two exports produce a column of that name whose values are not the labels a reader would expect. | 06-43 | Open — the substitution class of 06-16 |
| P-22 | No export derives `peak_channel_id` from an NWB units table; `jnwb/addressing.py:340` assumes the column exists. | 06-43 | Open — does not block the composition, whose caller supplies it **Claimed by 06-90.** |
| P-37 | **The root pattern: a proxy for an invariant was mistaken for the invariant.** Six instances, enumerated rather than counted, because the count was itself misreported once. (1) A checkout-provenance assertion standing in for "qualify the selected installation" -- P-39. (2) A `.git` *directory* check standing in for "a valid git administrative entry" -- P-40. (3) The first failing gate standing in for "complete gate-state observation" -- P-38. (4) Five hardcoded strings standing in for "the skill declares a coherent loading order" -- P-42. (5) A test whose case set is **generated by the thing under test**, so it cannot reach a value missing from the map that contains the defect -- P-50, a new shape rather than a repeat. (6) `.git` **existence by name** standing in for the same invariant as (2), which is P-40's own repair, together with the fixture that built the counterfeit and called it the clone -- P-56. Five of the six are this repository's author. **The enumeration grew again on 2026-09-20, by five, which is why this row closes on a quiet pass rather than on a repaired list:** (7) a skill's API route checked by `hasattr` on a submodule, where the invariant is membership of `__all__` -- P-59; (8) a role-name check reading `artifacts/agents/*.md` stems where the invariant is what `AGENTS.md` says, so the row it closed reproduces with the suite green -- P-64; (9) `artifacts/state.md` asserting that a gate enforces its freshness, where no gate reads it -- P-65; (10) a docs-call test checking unknown keywords and *too many* positionals as a proxy for "every documented call runs", passing while two documented calls raise -- P-71; (11) a test asserting the *absence* of UNRESOLVED where the invariant is that an unresolved row says why, so the honest output tripped the check -- P-86. A near-miss worth recording as not-an-instance: P-15 proposed trimming `AGENTS.md` toward a line count, where length is the proxy and duplication is the invariant; the measurement was taken first and the premise did not survive it. | Hamm, 2026-09-19, reviewing the first three | Open -- this row closes when the enumeration stops growing over one full pass, not when the current instances are repaired **Claimed by 06-60.** |
| P-34 | Eight measured exponents contradict `artifacts/benchmarks/complexity_inventory.md`, which records them as "verified": INV-05 claims +1.00 where a timed sweep measures **+2.14**; INV-06 claims +2.00 for two granger exports where they measure **+1.07** and **+1.11**; INV-08 claims +1.00 against **+0.62**; INV-13 claims +2.00 and +1.00 against **+0.54** and **+0.47**; INV-14 claims +0.00 and +1.00 against **+0.83** and **+0.49**. The likely cause is that the inventory was verified against heap allocations and never timed, so the two documents measure different things while using one word for both. Two sources disagree on the same quantity. | 06-54 | Open — both sides recorded, neither picked. A contradiction is surfaced, not resolved by the measurer **Claimed by 06-86.** |
| P-36 | Reading computational order off the source was wrong on six specs, in both directions: five where a source reading predicted a gap the measurement did not find, and one exponential case a reading of the loop structure would not have caught. A nested loop is not evidence of the order it looks like. | 06-54 | Open — a method finding worth a line in the documentation form contract or the developer role, not a code defect **Claimed by 06-87.** |
| P-28 | The worktree provisioner branched all three fan-out agents from `5ecc12eb`, **192 commits behind** the `f23d96ce` their packets named as baseline. `artifacts/goal.md`, `artifacts/problem_stack.md` and `artifacts/agents/jnwb-developer.md` did not exist there, the cited line numbers pointed at unrelated code, and the todo items themselves were absent. All three detected it independently and fast-forwarded. It is luck that they did: a packet that trusted its baseline would have measured the 0.1.8 tree and reported against it. **Reproduced live 2026-09-20, at the same commit.** The first agent dispatched after 06-91 was written was provisioned at `5ecc12eb` while its packet named `8a46d19e` -- **202 commits behind, measured by that agent with `git rev-list --count`, not the 192 of the original incident recorded above**; `git worktree list` from the main tree shows both. At `5ecc12eb` the item it was sent to execute does not exist, the three problems it claims do not exist, and `.claude/agents/jnwb-actor.md` -- the file its first instruction told it to read -- does not exist either. This is not a historical lesson about one bad fan-out: the provisioner does it again, and the only thing standing between it and a packet reporting against the 0.1.8 tree is that someone checks. | 06-10, 06-13, 06-15, independently | Open — harness, not repository; every future packet must verify its own baseline before reading anything **Claimed by 06-91.** |
| P-29 | `jnwb/compression.py` matches the corpus pattern with an unanchored `.search()`, so it selects any path whose tail contains the corpus name, in any group. Measured: 6 of 6 adversarial names are selected for lossy fp32 downcast, including `stimulus/probe_0_lfp/data`, `analysis/probe_0_lfp/data`, `scratch/backup_probe_0_lfp/data` and `acquisition/my_probe_0_lfp/data`. | 06-13 | Open — independent of the 06-13 ruling and repairable without it |
| P-30 | `compress_fp32` stamps its outputs with `conversion_script = "scripts/convert_nwb_compressed.py"`, which does not exist in the repository. Every compressed file carries provenance pointing at a script nobody can run. Carried from `alignment_review_0.2.5.md:611` and still true. | 06-13 | Open **Claimed by 06-66.** |
| P-26 | 7 documentation pages embed raster PNG figures from `docs/assets/figures/`. A documentation-form rule requiring inline SVG would forbid something 7 existing pages do, which is 06-48's own stated stop condition for a wrong rule. | This session, measuring for 06-48 | Open — narrowed by the 2026-09-19 ruling: the rasters stay, the inline-SVG rule stays removed, and 06-52 verifies theme-independence against the two schemes that now exist rather than converting formats. Closes when 06-52 does |
| P-33 | plotly 6.6.0 and kaleido 1.2.0 are installed on this machine and declared in no extra of `pyproject.toml`, so a CI docs build has neither. Ruled 2026-09-19: not added. Any figure work must therefore render without them, and a page that silently depends on a local plotly install would break only in CI. | This session | Open — closes when 06-52 confirms no documentation figure needs them |
| P-14 | `skills/jnwb-fact-action/SKILL.md` declares a Mandatory Authority Loading Order of `AGENTS.md`, `fact_stack`, `todo_stack`, domain skill, evidence. `AGENTS.md` §3 now loads eight, adding `goal.md`, `state.md` and `problem_stack.md`. Packets follow the skill, so they will not load three of the five `X` slots. Claimed by 06-61 (the ruling) and 06-70 (the evidence). Measured 2026-09-19 in `artifacts/authority_loading_order.md`: four candidate orders, and an adversarial example on this tree at this HEAD that splits them two against two on a real stop condition. The candidates are not the same order stated twice. **Ruled 2026-09-19: candidate C.** `AGENTS.md` §3 is the sole loading-order authority; the skill points at it and carries no list. Repaired, not closed -- the dispatcher wrote the repair, so 06-64 verifies it. | This session, creating it | Open — **Claimed by 06-64**, which verifies the repair the dispatcher wrote. The ruling itself is made; what is unverified is whether candidate C was implemented as ruled |
| P-15 | `AGENTS.md` is 436 lines against a contract requiring it to be a thin router that never duplicates project truth. **The premise is now measured and does not hold.** `artifacts/agents_md_duplication.md`: 181 claim-bearing sentences against 1365 elsewhere, **8 duplicated (4.4%)** and 10 echoed. Length is not the defect; eight specific claims with two homes are, six of them in the two sections whose job is to route. Trimming toward a line count would optimize the proxy, which is P-37's error. | This session | Open — restated as eight named duplicates, claimed by 06-72 |
| P-08 | Two findings that otherwise reproduce carry wrong counts: `doc-assets/module-map-omits-nwb-entry-points` says 36 omitted exports where a probe counts 35, and `ai-plumbing/agent-roles-exist-but-ship-nowhere` says five roles where six exist. | 06-03 | 06-17 |
| P-09 | A shadowing `jnwb` package and a `jnwb.release-backup` sit in `C:\Python314\Lib\site-packages\`. **Re-measured 2026-09-20 and the premise has changed: they are no longer byte-identical.** `jrsa.py` and `nwb_io.py` differ between the installed copy and this checkout, so a probe that omits the path insert now measures materially different code -- the jrsa fallback removal and the `allow_missing` work are both absent there. The row was `nothing is wrong now`; that is no longer true, and the hazard is live rather than dormant. | 06-04, re-measured this session | Open -- **Claimed by 06-81 and 06-35.** 06-81 makes the copy identifiable, which is the thing a probe needs; 06-35's clean-environment matrix is where the two copies stop being confusable in practice |
| P-10 | `.gitignore` excludes the 6.7 GB derived cache under `artifacts/developer/.cache/` by extension (`*.pkl`), not by path. A cache file written with any other suffix lands untracked and visible to a careless `git add -A`. | This session | Open — claimed by 06-73 |
| P-12 | The suite is collection-order fragile: an ad-hoc pytest subset can fail three `test_backend` tests and segfault. Pre-existing and unrelated to any current change. | Carried | Open — claimed by 06-74. It cannot stay carried: the release requires no `open` row, so this ends `repaired` or `accepted`, never deferred again |
| P-41 | `artifacts/fact_stack.md:58` reads "CI tests the declared floor and newest supported version." `AGENTS.md:200-202` records that exact policy as amended away on 2026-09-19, naming it as what let 0.2.5 ship a 3.13 classifier no CI leg exercised, and `.github/workflows/workflow.yml:41-46` runs `os: [ubuntu-latest, windows-latest]` against `python-version: ["3.12", "3.13", "3.14"]` -- **six** test legs covering all three declared versions, not floor-and-newest. (06-70 reported three legs; re-resolved, it is three versions across two operating systems.) **The contradiction is between two slots Hamm rules, not merely between a fact and the tree:** `artifacts/goal.md` states "Every claimed version is exercised in CI" and `artifacts/fact_stack.md:58` states the opposite policy, so an agent loading both slots in the order `AGENTS.md` §3 prescribes receives two authoritative and incompatible claims about the same thing. | 06-70, re-verified independently by the dispatcher | Open -- the `fact` slot is not agent-editable (`AGENTS.md` §2), so only Hamm closes this. Not a duplicate of P-C6, which covers the shipped artifact rather than the fact **Claimed by 06-92.** |
| P-42 | `tests/test_harness_adversarial_gates.py:641-652` asserts the authority loading order by checking that five hardcoded strings appear in `SKILL.md`. It therefore passes whenever the text matches that literal, whatever the order means, and it green-lights precisely the five-source list P-14 says is wrong. It stays green under two of the four candidate orders and fails under a third, so any 06-61 ruling either changes this test in the same commit or is contradicted by it. **Fourth instance of P-37.** | 06-70, re-verified independently by the dispatcher | Repaired 2026-09-19, pending verification -- the literal five-string assertion is replaced by two structural ones: the skill's *directive paragraph* must name `AGENTS.md` §3, and the section must enumerate no ordered source list of its own. A second test asserts the pointer's target reaches all five slots, because a pointer to an incomplete order is worse than a list. Three mutants killed in both directions Hamm named, each restored hash-identical. **One survivor found and fixed on the way:** matching the whole section passed a mutant that deleted the directive and left the paragraph *explaining* the drift, which also says "AGENTS.md §3" -- mentioning is not delegating, which is P-37 once more. Closes when 06-64 verifies |
| P-43 | The public API exports the error and not the remedy. `MissingRequiredNWBFieldError` is in `jnwb.__all__` and is the only symbol `docs/api.md` lists under "Module: jnwb.nwb_io"; `read_nwb`, `nwb_read_io`, `hdmf_build_repair_context` and `SqueezedAttributeWarning` are in neither `__all__` nor `dir(jnwb)`. `read_nwb(path, allow_missing=...)` is reachable only by importing the submodule directly, and is undocumented. **06-41 was ruled, implemented, and is unreachable from the public API** -- the caller whose report produced the ruling cannot use what was ruled. | 06-71, re-verified by the dispatcher | Open -- blocked on 06-67; the shape of the fix depends on what a waiver is ruled to mean **Claimed by 06-82.** |
| P-44 | `__version__` cannot identify which copy is under test. The installed copy and this worktree both report `0.2.5` while their `read_nwb` signatures differ -- the installed one raises `TypeError: unrecognized argument: 'allow_missing'`. Any provenance check reading `__version__` is blind to the difference, which weakens the `tests inspect the selected installation` invariant at exactly the point a probe is most likely to take a shortcut. | 06-71 | Open -- feeds 06-35's clean-environment matrix **Claimed by 06-81.** |
| P-45 | Integrity damage reads as absence. A dangling soft link and a broken external link are handled exactly as a field that was never recorded, and an `S2` string holding one NUL byte reads as genuinely empty. A file that was damaged is indistinguishable from a file that was incomplete. | 06-71 | Open -- three further collapsing states beyond 06-67's six; feeds that ruling |
| P-46 | A soft link to a valid description is refused: `MissingRequiredNWBFieldError` by default, and `ValueError: already exists in root.links` when waived, although the correct value is on disk and reachable. Refusing a file whose required field **is** present is a false refusal, not a conservative default. | 06-71 | Open **Claimed by 06-82.** |
| P-47 | The chunk shape is unconditionally 2-D, so `select=` cannot name a 1-D dataset. `jnwb/compression.py:293`, `:339` and `:354` all build `chunks = (min(N, shape[0]), n)` and hand it to `create_dataset` at `:172`; a 1-D selection raises `ValueError: 'chunks' must have same rank`. Latent today because nothing selects a 1-D dataset. **It blocks the option 06-13 is leaning toward** -- explicit caller selection admits 1-D datasets by construction. | 06-69 | Open -- not a reason to prefer another default; a reason to sequence this repair before `select=` is implementable **Claimed by 06-79.** |
| P-48 | The series the compression contract exists to preserve has no test protecting it. `tests/test_compression.py` names `convolved_spike_train` twice, both in fixture construction, and asserts nothing about its dtype. 06-69 measured three candidates that downcast it while passing 15 of 15 tests. "The existing contract survives R1, R2 and R4" is therefore a statement about three candidates that happen not to break an unenforced rule. | 06-69 | Open -- the contract needs a test before it can be said to survive anything **Claimed by 06-78.** |
| P-49 | `vflip` and `label_layers` disagree about what `crossover_contact` indexes, and nothing catches it. `jnwb/laminar.py:297` reorders `psd_arr[order]` **only when `probe_geometry` is supplied**; `:811-815` **always** builds `rank` from `probe_geometry.linear_order`; `:763-768` checks only the channel **count** across the boundary. A caller running `vflip` without geometry and `label_layers` with it crosses two index spaces: measured, **18 of 24 contacts receive a different layer, `accepted=True`, and no warning is emitted.** Passing geometry to both gives 0 of 24 differing. | 06-18, re-verified in source by the dispatcher | Open -- a defect under every available contract choice. If callers must supply geometry to both, the repair is a refusal at the boundary rather than a reorder; silently returning mislabelled anatomy is not an option in either reading **Claimed by 06-77.** |
| P-53 | `artifacts/todo_stack.md` named `Skill: jnwb-nwb-io` in items 06-69 and 06-71. **No such skill exists**; `skills/` holds jnwb, jnwb-connectivity, jnwb-fact-action, jnwb-figures, jnwb-lfp-spectral, jnwb-nwb-data, jnwb-population, jnwb-spiking and jnwb-statistics. The name went into both items and into two dispatched packets, nothing errored, and a packet worked around it silently. The standing rule is that a file pointing at other files goes stale without erroring -- except this pointer was never valid, so it did not go stale, it was **born wrong**, which no staleness check would have caught either. | This session, found by 06-71 | Open -- the two items are corrected; the row stays until a gate resolves every `Skill:` field against `skills/`, which is a mechanically preventable failure and therefore gate-eligible **Claimed by 06-80.** |
| P-54 | 9 of 22 real sessions type spike-train containers as `ElectricalSeries`, including an int16 dataset; 12 type the same logical series as `TimeSeries` and 1 omits `neurodata_type` entirely. No stack item claims this. | 06-69 | Open -- needs an item, or an explicit decision that it is the corpus's defect and not jnwb's **Claimed by 06-84.** |
| P-55 | Eight producers have no public consumer: `assign_outer_folds`, `build_inner_validation_partitions` (the decoder accepts no `groups` or fold column), `fit_exponential_onset`, `aperiodic_fit`, `xflip`, `zflip`, `consensus_bad_trials`, `detect_band_outliers`. 06-18's stop condition fired correctly and it did not propose them. | 06-18 | Open -- a capability question, not a defect report: a producer whose output nothing public consumes is either an unfinished chain or an export that should not be public **Claimed by 06-85.** |
| P-56 | **P-40 was closed `repaired` on a repair that was itself the proxy, and its own discriminator fixture agreed with it.** P-40 replaced "`.git` is a directory" with "`.git` exists by name". Existence by name is still the proxy: an empty directory `docs/.git`, or a zero-byte file, made any duplicate skill tree beneath it invisible to gate 2. The fixture that was supposed to catch this built its `nested-clone` case with a bare `(checkout_root / ".git").mkdir()` -- an empty directory, which is the counterfeit rather than the clone -- so the case named after a checkout was in fact the evasion, and it passed. **Sixth instance of P-37**, and the first where a closed `repaired` row is the instance. | 06-64, verifying P-40's repair | Repaired 2026-09-20, pending verification -- `_is_git_admin_entry` now requires a directory holding `HEAD` or a file whose first line is `gitdir:`. The fixture builds a real clone, two counterfeit cases were added, and `test_every_live_worktree_is_a_real_checkout` now rests on the same predicate as the gate rather than on `.exists()`. Three mutants killed, each failing exactly its corresponding case, restore hash-identical **Claimed by 06-83.** |
| P-57 | Nothing resolves a stack's cross-references, so a pointer to a deleted item reads exactly like a live claim. Measured on this tree: `06-62` was `Blocked by: 06-61` and `06-06` was `Blocked by: 06-01, 06-02`, and all three of those items were ruled on 2026-09-19 and deleted as complete -- two dispatchable items sat marked blocked by decisions that had already been made. In the other direction, P-05 and P-25 pointed at `06-10` and `06-48`, both deleted as complete, and both problems turned out to be answered; the column said `Open`. Same class as P-53 and the same standing rule -- a file that points at other files goes stale without erroring -- but here the stale pointer changes what gets dispatched. | This session, the elaboration pass over the five slots | Open -- **Claimed by 06-80.** The four stale pointers are corrected; the row stays until the resolution is mechanical, because correcting them by hand is what just failed to happen for a day |
| P-58 | **A skill instructs an agent to inject a 17 ms error into scientific output.** `skills/jnwb-spiking/SKILL.md:27` says causal smoothing delays the onset it measures, that `tau_ms=25` lands +17 ms late, and to subtract the filter delay. The skill routes to `fit_exponential_onset`, and to no threshold-crossing operation at all. Measured on a 1 ms step at true t0 = 100 ms: a half-amplitude crossing lands at 117.00 under tau=25, but `fit_exponential_onset` lands at **99.12 -- 0.26 ms of error**. An agent following the invariant subtracts 17 ms from a result that is already right. `docs/common_mistakes.md` §8 scopes the correction properly and names `fit_exponential_onset` as the *remedy*; the skill drops the scoping. | Skills critic, measured | Open -- **unowned, and the most consequential row on this stack.** Layer onsets are scientific output and the error is silent |
| P-59 | `skills/jnwb-nwb-data/SKILL.md:43` routes to `jnwb.nwb_inspect.CONTINUOUS_KEYS`: `nwb_inspect` is not in `jnwb.__all__` and `CONTINUOUS_KEYS` appears nowhere under `docs/`. The skill routes an agent at a private, undocumented constant. `tests/test_skills_validation.py:234` matches `jnwb\.([A-Za-z0-9_]+)` and asserts `hasattr`, so it checks the submodule and never `__all__`. **P-37 instance:** reachability by attribute is the proxy; being public is the invariant. | Skills critic | Open -- claimed by 06-24, whose acceptance does not currently mention publicity |
| P-60 | 210 default-bearing API restatements live in the nine skills. 179 sit inside the `## 2.` routing window and are machine-reconciled against the runtime; **31 do not and nothing reads them**. `skills/jnwb/SKILL.md` is ungated in its entirety, because its §2 is a delegation matrix carrying zero `jnwb.foo(...)` rows. The `direction.md` rule that a skill may not hold a mutable API fact is violated 210 times on its wording; the 31 are the live exposure. | Skills critic | Open -- **Claimed by 06-24.** The repair is to widen `_routing_matrix_section` to the whole file, which reuses machinery that already passes 116 rows |
| P-61 | Eight statements across six skills carry implementation authority rather than routing: joblib pool internals (`skills/jnwb:25`), the Welford accumulator and `10*log10(ratio)` (`jnwb-lfp-spectral:18,34`), the fitted exponential form (`jnwb-spiking:23`), Beta-quantile inversion (`jnwb-statistics:18`), the `location`-then-`area` resolution order (`jnwb-nwb-data:88`), and what `granger` wraps (`jnwb-connectivity:24`). `artifacts/goal.md` §2: a skill names an operation, documentation defines it. | Skills critic | Open -- unowned. Skill files are doctrine-adjacent, so the wording is proposed and not applied |
| P-62 | `skills/jnwb/SKILL.md:24` states that a GPU request the machine cannot serve "warns and runs on CPU" and that the result records the device. Measured with CuPy present and one CUDA device: `complex_tfr` records `cuda`; `rdm` records `cpu` and warns; **`relative_power` downgrades to CPU silently**; `spectral_tilt` and `wpli` carry no device field at all. | Skills critic | Open -- 06-56 owns the code repair but writes `jnwb/` and `tests/`; the false skill claim is unowned |
| P-63 | 30 public exports are named in no skill, including `TFRAnalyzer`, `UnitAnalyzer`, `PopulationAnalyzer` and the whole `Query`/`Question`/`Result`/`Interpretation`/`Lineage`/`Provenance` cluster -- an agent-facing subsystem no skill discovers. Coverage gap, not redundancy: the seven domain skills otherwise partition 110 operations with exactly one overlap. | Skills critic | Open -- unowned |
| P-64 | **P-C2 closed `repaired` on evidence that does not hold.** It recorded that `AGENTS.md` named five roles where six exist, and cited `tests/test_harness_adversarial_gates.py` as the check that would catch its return. That test asserts a hardcoded literal at `:775-776` against `artifacts/agents/*.md` stems and against the `ROLE:` enum. **It never reads `AGENTS.md`.** Renaming a role there reproduces P-C2 with the suite green. All three surfaces agree today, so nothing is currently wrong -- the guard is. **P-37 instance, and the second closed `repaired` row whose repair was a proxy** (P-56 was the first). | Doctrine critic | Open -- unowned |
| P-65 | `artifacts/state.md:4-5` asserts "a gate fails when this file no longer matches the tree, so a stale basis cannot be read as a current one." No gate reads it: `harness_gate.py` never mentions the file, and the gate ran 13/13 while the file was stale. The one test invoking `--check` asserts `"PASS" in out or "ERROR" in out`, which both outcomes satisfy. The sentence is emitted by `scripts/reconstruct_state.py:122`, so every regeneration re-asserts a protection that does not exist. **P-37, inside the `state` slot.** | Doctrine critic | Open -- unowned; no item in the stack mentions the state generator or its file |
| P-66 | **An authority above the `goal` slot that no loading order loads.** `artifacts/goal.md:4` says it is "Subject to `artifacts/direction.md`", and `direction.md:3-4` calls itself the durable product direction and a ruling of record. `AGENTS.md` never names the file -- not in §0's map, not in §2's slot table, not in §3 Prepare. Since 06-61 ruled §3 the sole loading-order authority, an agent following the sole authority never loads a ruling of record. **P-14's exact shape, on a source outside `X`.** | Doctrine critic | Open -- unowned |
| P-67 | The retired floor-and-newest CI policy has **three homes, not the one P-41 names.** Besides `artifacts/fact_stack.md:58`: `CONTRIBUTING.md:12` ("CI runs 3.12 and 3.14") and `CONTRIBUTING.md:34` ("the declared Python floor and newest supported version"). P-C3 closed this policy `repaired` in `AGENTS.md` and `harness_gate.py` and missed `CONTRIBUTING.md`. Four surfaces state it correctly, three do not. A contributor reading `CONTRIBUTING.md` today is told to ignore 3.13. **Found independently by two critics.** | Doctrine critic and code-docs critic | Open -- 06-92 is scoped to one sentence in the fact slot; the `CONTRIBUTING.md` instances are unowned |
| P-68 | `artifacts/goal.md:70` claims gate 8 enforces convergence across `requires-python`, classifiers, the CI matrix, the install documentation, the README and release material. Gate 8 reads three files: `.github/`, `.readthedocs.yaml`, `pyproject.toml`. No README, no `docs/install.md`. P-C4 shows `docs/install.md` was corrected **by hand**, which is the evidence that nothing mechanical holds it. **A proxy claim inside a Hamm-ruled slot.** | Doctrine critic | Open -- unowned |
| P-69 | `artifacts/problem_stack.md:10` said "Four dispositions close one" above a table whose first row is `open` -- "Not yet answered. **Blocks the release.**" Three close. `AGENTS.md:87` states it correctly. This file contradicted itself and §2. | Doctrine critic | Open -- repaired in the same pass that recorded it; retained as a row because the sentence was hand-written and nothing checks it |
| P-70 | **The contributor entry path contains three false instructions and one that contradicts itself.** `CONTRIBUTING.md:12` and `:34` carry the retired CI policy (P-67). `:47` says "All four run in CI"; three do -- `release_gate.py` is never executed by `workflow.yml`, which only imports `forbidden_entries` from it at `:109`. `:45-55` heads four checks with "**Before you push**" and then describes the fourth, 11 lines later, as "Only needed before tagging"; followed literally the documented pre-push sequence fails on a clean tree and needs network access. Volume is **not** the defect: the minimum honest entry set is `README.md` + `CONTRIBUTING.md`, 400 lines, and the critic judged it proportionate and sufficient. Four sentences are wrong. | Doctrine critic, path followed literally | Open -- unowned, and the cheapest high-value repair on this stack |
| P-71 | **Two documented calls raise `TypeError` on the line a reader copies, and the test that exists to catch that is green.** `docs/06_spikes_psth_and_onset_dynamics.md:166` documents `build_time_resolved_matrix(spike_matrices)` where the signature is `(session, area, epochs_df, ...)`; `:169` does the same for `compute_population_trajectory`. Both executed, both raise. Re-measured independently by the docs sweep, which found the block wrong in **three** ways at once: the input type (a dict of per-unit matrices, where a session/area/epochs triple is required), the return arity (documented 2-tuple, actual 3-tuple `(X, unit_ids, bin_centers)`), and the array shape (`jnwb/trajectory.py:40` gives `(n_trials, n_units, n_bins)`, the comment says `(n_trials * n_times, n_units)`). `docs/common_mistakes.md:312` uses the correct form, so two pages contradict each other. `tests/test_docs_call_shapes.py` passes 20 tests and its docstring claims "Every documented call shape must match the signature that ships" -- it checks unknown keywords and *too many* positionals, and never *too few*. **P-37 instance.** | Code-docs critic and docs sweep, independently | Open -- unowned |
| P-72 | `docs/10_operation_specifications.md:94` documents `zflip` as taking `cross_spec: (n_ch, n_ch, n_freqs)` in units of coherency, and does not mention `fs`. The signature is `zflip(lfp_matrix: (n_ch, n_times), fs, ...)` and the docstring states that pre-averaged C x C x F tensors are **rejected**. Executed: `ValueError: zflip requires a 2D array of shape (n_channels, n_samples); got shape (8, 8, 30)`. Line 50 of the same page describes it correctly, so the page contradicts itself. | Code-docs critic | Open -- unowned |
| P-73 | **The documented convention would make a contributor reintroduce a completed repair.** `docs/10:19` and `CONTRIBUTING.md:111` both prescribe `rng: Optional[Union[Generator, int]] = None`. Measured: 19 of 23 `rng` parameters deliberately do not present `None`, and `jnwb/_rng.py` records that moving the seed into the signature **was the 05-35 repair** -- five functions once declared `rng=None` and then ran `default_rng(42)` internally. The signatures are correct; the two convention texts are stale. | Code-docs critic | Open -- unowned |
| P-74 | Four parameter names in `docs/10` §2 do not exist: `probe_geom` for `probe_geometry` (`:91`), `corr_matrix` for `data` (`:93`), `coords` for `electrodes_table` (`:88`), and `n_times` where every synth callable uses `n_samples` (`:92`). §5 of that page is pinned by a test; **§2 -- the operation table, the page's reason for existing -- has no mechanical check over its Input and Shapes column**, which is why these and P-72 all survive. A candidate gate was written and run: 9 rows, 25 pairs, 4 violations, 0 false positives. | Code-docs critic | Open -- unowned |
| P-75 | `docs/api.md:11-15`: five constants render their Python builtin's docstring ("Built-in immutable sequence.") instead of a description, and `SKILLS_URL` is typed `function`. The defect is in `scripts/generate_api_md.py` (`_first_doc_line`, `_object_type_name`), not the page. 06-09 owns the `SKILLS_URL` type; the four other constants' descriptions stay wrong after it closes. | Code-docs critic | Open -- **Claimed by 06-09**, whose acceptance needs widening from "typed function" to "described as itself" |
| P-76 | `examples/quickstart_jnwb.py` writes two **tracked** files, and `docs/quickstart.md:55` tells the reader to run it. The SVG carries a `<dc:date>`, so every run dirties the checkout; a contributor who runs the documented example is left with a diff they did not author, in a repository whose rules forbid `stash`, `reset` and `restore`. The remedy was verified rather than assumed: `metadata={"Date": None}` alone is insufficient because matplotlib emits random `<path id=...>` hashes; **`rcParams["svg.hashsalt"]` plus `metadata={"Date": None}` produces byte-identical SVG**. PNG is already deterministic, which makes 06-29's premise -- that re-running reproduces none of its outputs byte-identically -- worth re-measuring. | Code-docs critic, which dirtied the tree doing it | Open -- 06-29 owns `docs/generate_figures.py`; `examples/quickstart_jnwb.py` is outside its scope |
| P-77 | **The dispatcher edited the tree while a read-only packet was reading it -- a second instance of P-C1.** `artifacts/problem_stack.md` changed under the code-docs critic mid-pass, and it reported the change rather than silently folding it in. P-C1 closed `repaired` on the sequencing rule "nothing writes the tree while a packet reads it"; the rule held for three hours and then failed the moment three of four agents had returned and the fourth was forgotten. A rule that depends on the dispatcher remembering how many agents are live is not a repair. | This session, reported by the code-docs critic | Open -- unowned. The mechanically preventable form is a dispatcher-side check that no packet is live before a write |
| P-78 | `docs/07_statistical_inference_and_nulls.md:12` states "All statistical resampling functions accept an optional `rng: np.random.Generator`" and `:55-61` demonstrates `exploratory_compare(..., rng=custom_rng)`. `exploratory_compare` has **no `rng` parameter** -- executed, it raises `TypeError: got an unexpected keyword argument 'rng'` -- while it does bootstrap and does return `mean_diff_ci`. The universal claim is false and the function it is demonstrated on is the counterexample: a reader following the page cannot make that comparison reproducible, and is not told so. | Docs sweep | Open -- unowned |
| P-79 | Three documented `visual_qc` calls raise. `docs/09:86` passes `group_by="area"` to `plot_unit_quality_distribution(units_df, session_ids=None, figsize=...)`, which takes no such keyword and no `**kwargs`. `docs/09:88-89` calls `plot_noise_vs_signal(lfp_segments, spike_trains)` where the signature is `(units_df, figsize)`, so the second positional binds to `figsize` and it dies inside matplotlib with `Bbox.from_bounds() missing 1 required positional argument`. `docs/09:92` passes a list to `compare_session_quality(sessions_comparison_df)`, which indexes it by string key. | Docs sweep | Open -- unowned |
| P-80 | `docs/09:110-111` tells the reader that `setup_vector_graphics()` sets `pdf.fonttype = 42` and `ps.fonttype = 42` for TrueType embedding. `jnwb/viz.py:23-25` assigns exactly three rcParams -- `svg.fonttype`, `font.sans-serif`, `font.family` -- and never touches either. Measured after the call: both remain at 3. **A reader who trusts this ships a PDF figure with Type-3 fonts**, which is the rejection every journal's figure checker exists to catch, and finds out at submission. | Docs sweep | Open -- unowned |
| P-81 | `docs/02_paths_addressing_metadata.md:172` documents `quality_class` as taking `'Good' \| 'MUA' \| 'Noise'`. The function assigns `'Good'`, `'Fair'`, `'Poor'` (`jnwb/metadata.py:232,233,241`) and its **own docstring at `:207` says so**. `'MUA'` and `'Noise'` are never produced by any path. A reader filtering `df[df.quality_class == 'MUA']` silently gets an empty frame -- no error, no warning, and a plausible scientific story about there being no multi-unit activity. The sibling claims on the same line (`is_valid`, `issue_flags`) are correct, which is what makes the wrong one easy to trust. | Docs sweep | Open -- unowned, and the quietest failure on this stack |
| P-82 | `docs/02:197-201` calls `assign_quality_tier(quality="good", trial_presence_fraction=0.95, snr=4.5)` with scalars. The signature is three `pd.Series` in and a `pd.Series` out; the scalar form raises inside pandas with a message about `Index(...)` and a built-in method, which points nowhere near the cause. The same call with Series returns `['unstable']`. | Docs sweep | Open -- unowned |
| P-83 | **The quickstart's last line raises, and the cause is an inconsistency in the result object rather than in the page.** `docs/quickstart.md:151` prints `float(jrsa_res.p)`. With `stats=True`, `JRSAResult.p` is a shape-`(1,)` array while `value`, `statistic` and `ci` are all 0-d, and `float()` on a 1-element array is an error under NumPy >= 2 -- which `pyproject.toml:42` (`numpy>=1.26.0`) admits. The page is correct about every sibling field and wrong about the one shaped differently from them. Repairing the page leaves `p` asymmetric with its own siblings; repairing the field is a public-API change. | Docs sweep | Open -- unowned. **Newcomer path**, and the disposition needs a ruling on which surface is wrong |
| P-84 | **Four of the calls above are unknown-keyword or wrong-type errors, which is exactly what `tests/test_docs_call_shapes.py` says it checks** (P-78 `rng=`, P-79's `group_by=`, P-82's scalars, P-71's dict). P-71 explains the too-few-positionals gap in the *assertions*; it does not explain these, so the corpus that test walks must not reach `docs/02`, `docs/07` and `docs/09`. **The cause is inferred and not yet measured** -- the check to run is which pages and which block kinds the module actually collects. Recorded separately from P-71 so that repairing the assertion is not mistaken for repairing the coverage. | Inferred from the docs sweep; cause unmeasured | Open -- unowned |
| P-85 | `jnwb/jrsa.py:215` documents the `correction` argument as `holm-sidak \| fdr_bh \| fdr_by \| cluster \| maxT`. **`cluster` and `maxT` are not implemented and never were**; both raise `ValueError: Unrecognized correction method`. Re-resolved after the 06-76 merge, against this checkout. The docstring also omits `bonferroni`, `holm` and `none`, which all work -- so it is wrong in both directions at once. Permutation-based cluster and maxT correction are the two methods a reader would most plausibly want here, which makes the false advertisement worse than an omission. | 06-76 actor as U-2, re-resolved by the dispatcher | Open -- unowned. A docstring is not covered by the routing-matrix gate, so nothing catches it |

## Closed

| ID | Problem | Disposition | Evidence |
|---|---|---|---|
| P-31 | `jrsa.py` mapped an unrecognised correction through `_CORRECTION_METHOD_MAP.get(m_lower, "fdr_bh")`, so `correction="none"` passed the validity check and was then silently Benjamini-Hochberg corrected under the label `none`. Recorded as latent rather than live. | `repaired` | Superseded by P-50 on scope and **vindicated on its premise**: P-50 re-framed the substitution as live on the main path, and the 06-76 actor measured that it is not -- both `jrsa()` call sites short-circuit on `correction.lower() != "none"` and `_multiple_correction` is unexported. This row's original "latent" reading was the correct one. The code defect it names is repaired with P-50. |
| P-50 | `'none'` was absent from `_CORRECTION_METHOD_MAP`, so `.get('none','fdr_bh')` returned Benjamini-Hochberg q-values under the label `'none'`, and with statsmodels absent a request for *no* correction raised `ImportError` demanding the library that does correction. `tests/test_jrsa_correction_fallback.py:73` derived `SUBSTITUTED_METHODS` from `_CORRECTION_METHOD_MAP` itself, so **the test's case set was generated by the structure containing the defect** and could never cover a value missing from the map. | `repaired` | `'none'` is now a key of the map with value `None`, the lookup is `[m_lower]` rather than `.get(..., 'fdr_bh')`, and the `None` branch returns before the statsmodels import. The test is rebuilt around a literal `ACCEPTED_CORRECTIONS`. **The proof is M2 against M3:** the same source defect reintroduced under a test that re-derives its cases from the map passes, and under the literal set fails. Seven mutants killed; restores hash-verified (`jrsa.py=7e08e02f84ef`, `test_…=d7fdaa7458e1`). Verified after merge: `_multiple_correction(p,'none',0.05)` returns `[0.01,0.2,0.5]` unchanged where `'fdr_bh'` returns `[0.03,0.3,0.5]`. **Closed on the generated-case-set finding, not on main-path reachability** -- that half of the row was false and is recorded as such. |
| P-51 | The test named "THE DISCRIMINATOR for 06-15" asserted nothing on the repaired code: `test_holm_never_returns_the_benjamini_hochberg_values` caught `ImportError` and returned before its assertion, so the name claimed an enforcement it no longer provided. | `repaired` | Restructured in the same pass as P-50, so the name and what runs agree. Real enforcement remains `test_an_unavailable_estimator_raises_instead_of_substituting`. |
| P-52 | `p_flat * len(p_flat)` in the bonferroni fallback where statsmodels uses `pvals * float(ntests)`. | `repaired` | Now `p_flat * float(len(p_flat))`, bit-identical for float input. The row's stated mechanism was wrong and was corrected before closing: under NEP 50 on NumPy 2.4.6 an integer p-array does not wrap, it raises `OverflowError`. The defect was real, its failure mode was not as recorded. |
| P-86 | **The suite could not run in a fresh worktree, and two of its tests were proxies.** `test_every_row_resolves` in `tests/test_state_reconstruction.py` asserted that no row of `artifacts/state.md` is UNRESOLVED -- stricter than the invariant in its own docstring, and false wherever a value legitimately does not exist. A linked worktree has no upstream, so the Upstream row is *correctly* unresolved and the suite failed for it in every agent worktree, for a reason unrelated to the agent's work. Separately, `test_every_repository_path_agents_md_cites_exists` failed on `artifacts/state.md`, which is generated and git-ignored and therefore absent until its generator runs -- the normal state of every fresh checkout. | `repaired` | The first is now `test_an_unresolved_row_says_why`, asserting that an unresolved row names its cause, which is what distinguishes "not measurable here" from "the measurement broke"; the generator emits `UNRESOLVED (exit …)`, `UNRESOLVED ({type})` and `UNRESOLVED (file absent)`, all parenthesised. The second gained a carve-out narrowed to *git-ignored AND named as an output by some `scripts/*.py`*, plus `test_the_generated_path_exemption_does_not_excuse_an_ordinary_missing_file` as its discriminator. Verified by moving `state.md` aside and re-running: 2 passed, file restored. **Asserting absence was a proxy for "no row lies" -- P-37 again, with the honest output tripping the check.** Found by the 06-76 actor as U-5; repaired before the lanes launched, so five lanes did not each rediscover it. |
| P-05 | Nothing failed if the CI matrix and `PYTHON_CI_REQUIRED` were shrunk together: every assertion was a containment, so the untested-classifier gap could return with the suite green and gate 8 printing "all agree". | `repaired` | Gate 8 check 5 reads `pyproject.toml` and `workflow.yml` against each other rather than each against a constant, so no edit to a constant can make the gate agree with a wrong tree. A floor-versus-classifiers check was added 2026-09-20. Both proven by mutation rather than by passing: the `xv` mutant moves `requires-python`, `PYTHON_FLOOR` and `PYTHON_SUPPORTED` together so every constant-versus-constant check still agrees, and is caught; the `decoy` mutant inserts a second `python-version` matrix above `jobs.test`, which defeats a regex taking the first match, and is caught because the matrix is now read at `jobs.test.strategy.matrix` by path. Pristine established first on the same probe. |
| P-25 | 10 of the 27 documentation pages are not editable prose -- `docs/api.md` is generated and the 9 `docs/tutorials/*.md` are `--8<--` wrappers -- so a verbosity item handed the full set would edit 10 pages whose content lives elsewhere and whose edits the next build discards. | `repaired` | The carve-out exists in both places it was needed. `docs/documentation_form.md:18-24` tabulates the generated and included pages and states that the contract binds the 17 authored pages, with a fix to a generated page going to its generator; `:57-58` gives both classes no length ceiling, with the reason. 06-51 carries the same count in its own body. |
| P-11 | `git fetch` aborts on this clone: the v0.1.x tag objects are missing, so `git rev-list -n1 v0.2.5` fails and tags must be resolved through `git ls-remote` or `gh`. | `accepted` | **The remote is intact.** `git ls-remote --tags origin` returns `v0.1.0`, `v0.1.1` and `v0.1.3` with both the tag object and its peeled commit, so the missing objects are this working clone's alone and a fresh clone does not reproduce the failure. Nothing in the repository is wrong and no published artifact is affected. The operational consequence is bounded and already the standing method: every tag-dependent step uses `gh` or `git ls-remote`, which is the path CI takes regardless of any local clone. The hostile reader's question is why not re-clone, and it has an answer rather than an excuse -- re-cloning is available to anyone who needs `git fetch` here, it repairs nothing in the repository, and doing it now would discard the working state this release is being assembled in. |
| P-13 | The Write tool refused a `.md` deliverable inside a packet's own declared `Writes` scope, treating it as a report file. The packet reached its acceptance through a generator script instead. | `accepted` | The constraint is in the tool, which this repository neither owns nor can patch, and it is not reachable from any jnwb surface. No artifact is wrong because of it: the packet produced the deliverable its item declared, by a different route. What is accepted is an indirection in a packet's method, not a defect in the repository, and the record is the useful part -- the next packet reaches for the generator immediately instead of spending a turn discovering the refusal. |
| P-32 | jnwb's own Benjamini-Hochberg (`StatisticalAnalysis.fdr_correct`) and statsmodels' differ in the last ULP: `0.23225510313289427` against `0.2322551031328943`. | `accepted` | Both are correct Benjamini-Hochberg; they differ by one unit in the last place, which is the expected consequence of a different but equally valid order of floating-point operations. Repairing it means reordering one implementation's arithmetic to match the other bit-for-bit, which buys nothing scientifically and makes jnwb's arithmetic hostage to a dependency's internal ordering. Still live and not obsolete: `jnwb/statistics.py:227` and `:671` both ship the implementation, verified 2026-09-20. The record is the point -- any cross-check asserting bitwise equality between the two will fail, and this row is why it failed. |
| P-35 | 30 of the 49 order gaps 06-54 identified are invisible to its method: a gap of the form T(u*e) against T(u+e) reads as linear in `e` under any sweep that holds `u` fixed, so a flat exponent in that group is not evidence against the gap. | `accepted` | A property of the measurement method, not of the repository, and stated by the work that found it rather than caught in it afterwards. Resolving the 30 needs a two-parameter surface. Release condition (2) is that code matching its own official documentation is sufficient and need not be separately tested, so an exhaustive order surface is not a release requirement and building one to close this row would be scope the condition does not ask for. The inventory is complete for what its method could see and says explicitly what it could not. **What this does not cover:** P-34's eight exponents were measured and disagree with a document calling them verified. That is a contradiction rather than a limitation, it is not accepted, and 06-86 claims it. |
| P-C1 | A read-only packet's basis was invalidated because this session kept editing the tree while it ran. | `repaired` | Sequencing changed: nothing writes the tree while a packet reads it. Re-run of 06-04 on `026b9a6f` confirmed the tree stable at both ends. |
| P-C2 | `AGENTS.md` named five agent roles where six exist on disk. | `repaired` | `026b9a6f`; `tests/test_harness_adversarial_gates.py` asserts the role set and now also the `ROLE:` enum. |
| P-C3 | `AGENTS.md` and `scripts/harness_gate.py` stated a floor-and-head CI policy that the constant beside them had already replaced. | `repaired` | `265eb01e`. |
| P-C4 | `docs/install.md` made no Python claim while `artifacts/goal.md` asserted the install documentation converges on the supported set. | `repaired` | `026b9a6f`; the page now carries the same sentence as `README.md:41`. |
| P-C5 | `tests/test_skills_validation.py` docstring claimed eight canonical skills against a set of nine. | `repaired` | `026b9a6f`. |
| P-07 | `artifacts/todo_stack.md` item 06-03 said the review graded `public-claims/classifier-3-13-never-tested` as a broken gate; the review files it in the unverified tail at `low`. | `repaired` | The sentence lived inside 06-03, which is complete and therefore deleted (`9a11ffcf`). The ledger dispositions that identifier against the live tree instead. |
| P-27 | Gate 2 globbed `SKILL.md` off the filesystem, not the git index, so every agent worktree under `.claude/worktrees/` read as a duplicate skill tree. It failed early: 36 violations, the gate aborting at gate 2, 1 PASS line where 13 are expected, and three suite tests red. The harness gate and the batch barrier were both unusable during any fan-out, which is the working method for the rest of this release. Gate 4 then rejected `.claude` as an unauthorized root directory -- a second instance of the same cause, hidden until gate 2 stopped aborting ahead of it. | `repaired` | Gate 2 now excuses a nested git checkout, detected by its `.git` entry rather than by a path literal. `.claude` went into a new `TOOL_ROOT_DIRS` and deliberately **not** into `EPHEMERAL_ROOT_DIRS`, which gate 2 skips wholesale: `.claude/skills/` is a real location an agent reads skills from, and excusing the whole directory would have been the smaller edit and would have opened the hole gate 2 exists to close. `tests/test_gate2_ignores_nested_checkouts.py`, 11 tests, holds the pair property -- a worktree passes, a plain duplicate, an untracked duplicate and a non-checkout `.claude/skills/` tree all still fail. Discriminator run: with the skip disabled, 4 fail and the 3 that must hold regardless still pass; restore verified by md5, not by eye. Gate now reports 13 PASS with four worktrees live. |
| P-16 | This machine ran pandas 3.0.5 while hdmf 4.3.1 declares `pandas<3`: 20 of 21 requirements satisfied, one violated, so every green run here was in a configuration hdmf declares unsupported while CI resolved pandas 2.x. | `repaired` | Ruled 2026-09-19: pin the development environment rather than restate a bound jnwb does not own. `pip install "pandas<3"` resolved 2.3.3, and `python -m pip check` now reports "No broken requirements found" -- 21 of 21. This machine and CI now test the same dependency set. |
| P-17 | A consumer's 9 of 22 NWB files were refused for a missing `session_description`, on an implementation detail rather than a frozen rule: `goal.md` §4 covered empirical data in an analysis path, not required metadata. | `repaired` | Ruled 2026-09-19, both parts. §4 widened to cover required metadata, with a bounded exception. `read_nwb(path, allow_missing=("session_description",))` added; the default is still refusal. **The ruling as worded could not be implemented literally** -- pynwb takes `session_description` as a required positional argument of `NWBFile.__init__`, so waiving jnwb's check only hands the refusal to pynwb. The file cannot open unless something supplies a value. jnwb supplies the empty string: falsy, unmistakable for a real description, and recorded on the returned object as `jnwb_waived_requirements`. The deviation is written into `goal.md` §4 and needs confirmation. `tests/test_nwb_read_tolerance_and_visibility.py`, 11 tests over the measured 2x2. |
| P-18 | The length-1 attribute squeeze was silent, so a receipt written from a repaired file could not record that the file needed repairing. | `repaired` | Ruled 2026-09-19: warn. `SqueezedAttributeWarning` names the attributes, one warning per read rather than per attribute -- a malformed file can carry hundreds, and a warning nobody can read through is a warning nobody reads. A clean read raises none, asserted. |
| P-23 | `jnwb/nwb_io.py` opened "Module-internal: not part of `jnwb.__all__`" while the exception it defines is exported, documented in two pages, and asserted public by two tests. | `repaired` | The sentence now states the exception, so a reader hitting the raise is not told it is uncatchable. |
| P-24 | `mkdocs.yml` declared one palette scheme and no toggle, so 06-52's acceptance named a comparison the site could not make and a figure hardcoding a light background was unfalsifiable. | `repaired` | Ruled 2026-09-19: add a light scheme with a toggle. `mkdocs.yml` now carries `slate` and `default`, each with a toggle, keyed on `prefers-color-scheme` so a visitor's OS picks the default. 06-52's discriminator is executable as written. |
| P-38 | The harness ran fail-fast: the first failing gate returned, and gates after it did not execute. Gate 2's false positive on agent worktrees hid gate 4's genuine defect of the same class, printed 1 PASS line where 13 were expected, and three packets reported the resulting red suite as unrelated without establishing that the later gates had run at all. A first failure makes every later gate *unknown*; the workflow treated unknown as observed. | `repaired` | `run_full_preflight` is now table-driven and collect-all. Every gate runs whatever the ones before it did; a gate that raises is reported `ERROR` and does not stop the rest; a gate that does not execute is named `NOT RUN` rather than vanishing; the run ends in an `OVERALL:` line stating how many of 13 executed. The contract is pinned as `PASS <=> every gate executed and every gate passed`. `tests/test_every_gate_runs.py`, 18 tests, including one parametrized over all 13 that seeds each gate failing in turn and asserts the other 12 still ran -- so no gate can be positioned such that its failure hides another. Recorded by Hamm, 2026-09-19. |
| P-39 | Two test modules written on 2026-09-19 prepended the checkout to `sys.path` and asserted `jnwb` resolved under it. That makes the suite incapable of qualifying an installed wheel, which is the environment `tests/test_the_suite_can_qualify_an_installed_copy.py` exists to support. The existing scanner caught the prepend; nothing caught the provenance assertion. | `repaired` | Invariant restated from "append rather than prepend" to **tests inspect the selected installation; tests do not select the installation**. Three scanners added: one for the other in-process forcing spellings (`sys.path[0] =`, rebinding by concatenation), one forbidding any assertion that `jnwb` resolves under the checkout root outside the single designated harness, and one holding that harness to still existing and still honouring `JNWB_EXPECTED_PACKAGE_ROOT`. Subprocess `PYTHONPATH` injection is deliberately **not** flagged: six modules do it so a child process chooses what it measures, which is those probes' purpose, and a blanket rule would have been a third proxy. Two pre-existing uses of `jnwb.__file__` compare it against another runtime value and hold in both modes; they are not violations and the rule does not flag them. `test_state_reconstruction.py` now skips rather than asserts when the package under test is not this checkout. Recorded by Hamm, 2026-09-19; the prepend caught by the existing scanner. |
| P-40 | Gate 4 encoded `.git` as a directory. That is one representation: a clone carries a directory, a linked worktree carries a one-line file pointing at the real git directory. The gate's actual requirement is "a valid git administrative entry", so it rejected every worktree and aborted before gates 5 through 13. | `repaired` | Gate 4 tests for the entry by name and not by filesystem type, and `_is_inside_nested_checkout` uses existence rather than `is_dir`, so both representations are handled. Discriminator fixtures cover both: `tests/test_gate2_ignores_nested_checkouts.py` parametrizes linked-worktree and ordinary-clone shapes for gate 2 and again for gate 4, and asserts an unknown root file and an unknown root directory are still rejected, so the allowlist widened by exactly one name rather than into a policy of tolerance. **Superseded 2026-09-20 by P-56: this repair was itself the proxy.** Existence by name excuses an empty directory named `.git`, and the `ordinary-clone` fixture built exactly that, so the discriminator agreed with the defect. The row stays `repaired` because the defect it names was genuinely fixed; the repair's own defect is a separate open row rather than a rewrite of this evidence. Recorded by Hamm, 2026-09-19; symptom reported independently by three packets. |
| P-C7 | The `state` slot of `X` had no artifact, so the same basis was reconstructed twice in one day into agent transcripts the next packet could not read. | `repaired` | `scripts/reconstruct_state.py` generates `artifacts/state.md`; `--check` reports staleness in 0.3s; `tests/test_state_reconstruction.py` drives both. |
| P-C6 | The published v0.2.5 artifact carries a 3.13 classifier no CI leg exercised. | `accepted` | Unrepairable by definition: a shipped artifact cannot be retested retroactively, and rewriting immutable historical material to look correct was ruled out on 2026-09-19. The release body's "Python 3.12 through 3.14" is true of declared support and was false of tested support when it shipped. Only a subsequent release changes the fact. |
